// vue.config.js

/**
 * @type {import('@vue/cli-service').ProjectOptions}
 */
module.exports = {
    // 详细配置查询vue cli官网
    // publicPath默认值为'/' 默认架设在根路径上
    // 修改为'./'后可以将网页部署在任意位置
    publicPath: './'
}